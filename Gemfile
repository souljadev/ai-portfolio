source "https://rubygems.org"

# Matches GitHub Pages build (bundles Jekyll + approved plugins)
gem "github-pages", group: :jekyll_plugins

# Needed for `jekyll serve` on Ruby 3+
gem "webrick", "~> 1.8"
